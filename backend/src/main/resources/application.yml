spring:
  application:
    name: libraryApi
  datasource:
    url: jdbc:mysql://mysql-library:3306/library
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
#    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
#      ddl-auto: none
    properties:
      hibernate.format_sql: true